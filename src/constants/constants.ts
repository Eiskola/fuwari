export const PAGE_SIZE = 8;

export const LIGHT_MODE = "light",
	DARK_MODE = "dark",
	AUTO_MODE = "auto";
export const DEFAULT_THEME = AUTO_MODE;

// Banner height unit: vh
// Increased banner sizes to enlarge homepage banner display area
// BANNER_HEIGHT: visible banner portion in viewport height (vh)
// BANNER_HEIGHT_EXTEND: extra extension used to create a fuller/bleed look
export const BANNER_HEIGHT = 60;
export const BANNER_HEIGHT_EXTEND = 50;
export const BANNER_HEIGHT_HOME = BANNER_HEIGHT + BANNER_HEIGHT_EXTEND;

// The height the main panel overlaps the banner, unit: rem
export const MAIN_PANEL_OVERLAPS_BANNER_HEIGHT = 20;

// Page width: rem
export const PAGE_WIDTH = 75;
